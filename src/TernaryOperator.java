public class TernaryOperator {
    public static void main(String[] args) {
        /*
         * Ternary operator is shorthand for if statement!
         */
        var nilai = 90;

        String graduateStatus = nilai > 75 ? "Congrats! You Passed" : "Try Again Next Time!";

        System.out.println(graduateStatus);
    }
}
